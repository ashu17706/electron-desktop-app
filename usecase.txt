{
  _id: // autogenerated
  name: 'Some Name',
  imso_no: '12313',
  storage_rack: 'C1',
  lot_wise_quantity: {
  '1':  {
      lot_no: 1,
      quantity: 100,
      expiryDate: 'Some Date'
    },
  '2':  {
      lot_no: 1,
      quantity: 100,
      expiryDate: 'Some Date'
    }
  }
}


// in this case delete lot
// working

// medicines action

// total quantity

// creating new accept lot
  // create a new object with lotId as the key and value as above

// update // NOT supported for the first revision
  // delete any particular medicine from the lot
    // previously there was a, b, c
    // user removed c

// delete accepted lot
  // delete particular lot
  // remove all the lot keys from every medicines lot_wise_quantity


// dispatch lot
// create dispatch lot record
    //  dispatch 100 medicine x from lot no 1
    // disptach 20 medicine from lot no 2

// delete particular dispatch lot
  // delete the dispatch lot record
  // add dispatched medicine quantity back to the reduced lot record

// disptach lot
{
  lotno: 1,
  date: 'Some Date',
  lot_description: 'Some Description / Note',
  medicines: {
    'med1' : {
      lot: 1,
      qty: 100,
      expiry: 'Some Date'
    },
    'med 2': {
      lot: 1,
      qty: 100,
      expiry: 'Some Date'
    }
  }
}
